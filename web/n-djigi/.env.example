# Mode démo : true = tout fonctionne sans backend (données mock)
#             false = appels réels vers le backend Node
VITE_DEMO_MODE=true

# URL du backend Node (utilisée uniquement si VITE_DEMO_MODE=false)
VITE_API_URL=http://localhost:3001
